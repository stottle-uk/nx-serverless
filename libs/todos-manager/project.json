{
  "sourceRoot": "libs/todos-manager/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/todos-manager/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/libs/todos-manager"],
      "options": {
        "jestConfig": "libs/todos-manager/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
